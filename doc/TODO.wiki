[=Syntax=]

- maybe rename operators:
-- [.spo|§<.] to [.spo|<|.]
-- [.spo|§>.] to [.spo||>.]
-- or just use [.spo||.] instead of [.spo|..] for pipe to the right and do not support pipe to the left
--- [.spo|3 |+ 4 |* 10.]
- lambdas with [.spo|....] notation
-- [.spo|(=> ... .+ ...).] = [.spo|((a, b) => a .+ b).]

[=Types=]

- make types to first class citizen
-- can't uses generic types through module boarders now (do i need high order types)
- if lambda is used as argument in a other (higher order) function, infer the argument type
-- Example: [.spo|ListOfNumbers .Map (a => 2 .* a).] aka [.spo|ListOfNumbers .Map (=> 2 .* ...).]
- arrays ??? [.spo|[[v1, v2, ...]].]
-- maybe i need numbers in type signature first (for fix size arrays)

[=Misc=]

- imperative
-- [.spo|§IF...THEN...ELSE....]
--- [.spo|§LOOP....]
---- [.spo|§BREAK.]
---- [.spo|§CONTINUE.]
--- [.spo|...§LOOP....]
---- [.spo|§BREAK....]
---- [.spo|§CONTINUE....]
-- runtime environment
-- memory management
--- static optimized reference counting (RC)
---- use borrow and ownership to reduce RC operations

[=Road Map=]

- self hosted
- concurrency
	
[=Nice to have=]

- performance

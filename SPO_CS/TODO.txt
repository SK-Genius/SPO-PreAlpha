NEXT:
=====
	o remove x.ToString() ???
	
	o type system
		- opcodes for types
		- bind types to elements (mybe with an assert opcode)
			|	a € tInt32 := 4
	
	o lambdas with "..."
		|	(=> ... .+ ...)  ==  ((a, b) => a .+ b)
	
	o imperative
		- arrays
		- loops


ROADMAP:
========
	o self hosted
	o runtime type check
	o compiletime type check
	o concurency


NICE TO HAVE:
=============
	o performance
	o mIL_VM TraceOutput -> debugger infrastructur
